{
  "name": "percy-cypress-test",
  "version": "1.0.0",
  "description": "Test project for Percy Cypress migration",
  "scripts": {
    "test": "cypress run",
    "test:visual": "percy exec -- cypress run",
    "cypress:open": "cypress open"
  },
  "dependencies": {
    "@percy/cypress": "^1.0.0",
    "cypress": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0"
  }
}
